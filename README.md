## Usage with Backed CLI

in your backed.json or package.json file

```json

{
  "bundles": [
    {
      "plugins": {
        "inject-templates": {
          "exclude": "node_modules"
        }
      }
    }
  ]
}
```
